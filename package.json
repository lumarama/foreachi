{
  "name": "foreachi.com",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "vuepress dev src",
    "build": "vuepress build src",
    "commit-all": "git add -A && git commit -m 'new deployment'",
    "push-origin": "git push origin",
    "deploy-gh": "git subtree push --prefix dist origin gh-pages",
    "deploy": "npm run build && npm run commit-all && npm run push-origin && npm run deploy-gh"
  },
  "author": "Illya Sikeryn",
  "license": "ISC",
  "devDependencies": {
    "@vuepress/plugin-google-analytics": "^1.3.1",
    "vuepress": "^1.3.1"
  }
}
